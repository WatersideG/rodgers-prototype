# Rodgers Ski & Sport — Design Prototype

Clickable, responsive design prototype for the rodgersskiandsport.com rebuild
(spec of Aug 5, 2026). Nanmo layout language on the Rodgers brand navy (#012D5D).

- Open `prototype/index.html` — all 22 launch pages are linked.
- Photo blocks are direction placeholders pending the content-bank shoot.
- Orange accent is a stand-in pending the branding pass.
- Dashed "confirm" tags mark the spec's open ownership decisions.

Built as static, self-contained HTML — no build step, no dependencies.
